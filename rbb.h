#ifndef _rbb
    #define _rbb

    /*
    Recursive binary search
        v -> search vector
        left -> leftmost element position
        right -> rightmost element position
        val -> value to be searched
    */
    int rbb(int *v, int left, int right, int val); 


#endif